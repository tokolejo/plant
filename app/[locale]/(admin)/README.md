# Admin Route Group: `app/[locale]/(admin)`
This group handles administrative and moderation tools:
- `/admin` - Platform metrics & overview
- `/admin/moderation` - Review flagged listings and reported messages
- `/admin/users` - User tier overrides and verification
- `/admin/categories` - Plant and inventory taxonomy tags
